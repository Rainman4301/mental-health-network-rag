# ---------------------------------------------------------------------------
# requirements.txt — ETL pipeline dependencies
# ---------------------------------------------------------------------------

# Scraping
# NOTE: chromedriver is no longer resolved at runtime via webdriver-manager —
# it's baked into the Docker image at build time (see Dockerfile). This keeps
# DAG runs from depending on network access to Google's CDN every execution.
selenium==4.21.0
selenium-stealth==1.0.6
beautifulsoup4==4.12.3

# Text / NLP
emoji==2.11.1
emot==3.1
nltk==3.8.1
spacy==3.7.5

# Data
pandas==2.2.2
numpy==1.26.4
pyarrow==16.1.0          # Parquet read/write

# Embeddings + vector index (script 2)
sentence-transformers==3.0.1
faiss-cpu==1.8.0          # swap for faiss-gpu if running on GPU

# Azure Blob Storage
azure-storage-blob==12.20.0

# Utilities
tqdm==4.66.4
python-dotenv==1.0.1